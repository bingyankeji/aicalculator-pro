# 计算器页面设计规范文档 v1.0

**文档目的：** 为所有计算器页面提供统一的设计标准和实现规范

**适用范围：** AICalculator.pro 所有计算器页面开发

**更新日期：** 2024-11-17

---

## 📋 目录

1. [核心设计原则](#核心设计原则)
2. [SEO优化标准（100分满分）](#seo优化标准)
3. [页面结构规范](#页面结构规范)
4. [结构化数据规范](#结构化数据规范)
5. [FAQ撰写规范](#faq撰写规范)
6. [内链和外链规范](#内链和外链规范)
7. [UI设计规范](#ui设计规范)
8. [移动端响应式设计](#移动端响应式设计)
9. [输入字段设计规范](#输入字段设计规范)
10. [分享和导出功能](#分享和导出功能)
11. [域名配置规范](#域名配置规范)
12. [开发检查清单](#开发检查清单)

---

## 核心设计原则

### 1. **SEO优先，用户体验至上**
- 满足Google 100分SEO标准
- 保持用户界面简洁专业
- 移动端优先设计

### 2. **功能完整，透明可信**
- 所有计算参数清晰展示
- 提供完整的导出功能
- 教育性内容丰富

### 3. **统一标准，易于维护**
- 使用统一的组件和Hook
- 遵循配色和布局规范
- 代码可复用性强

---

## SEO优化标准

### 基础SEO（必需）

#### 1. Meta 标签配置
```tsx
export const metadata: Metadata = {
  title: '主要关键词 + 功能优势 + AICalculator',  // 50-60字符
  description: '行动导向描述 + 目标关键词',      // 150-160字符
  keywords: [
    // 20+个高价值关键词，包括长尾关键词
    'primary keyword',
    'secondary keyword',
    'long tail keyword phrase',
    // ... 更多关键词
  ],
  authors: [{ name: 'AICalculator.pro Team' }],
  creator: 'AICalculator.pro',
  publisher: 'AICalculator.pro',
  openGraph: {
    title: 'Calculator Name',
    description: '...',
    type: 'website',
    url: getUrl('/calculator-path'),
    siteName: 'AICalculator',
    locale: 'en_US',
    images: [{
      url: getOgImage('calculator-name'),
      width: 1200,
      height: 630,
    }],
  },
  twitter: {
    card: 'summary_large_image',
    title: '...',
    description: '...',
    images: [getOgImage('calculator-name')],
    creator: '@aicalculator',
  },
  alternates: {
    canonical: getUrl('/calculator-path'),
  },
  robots: {
    index: true,
    follow: true,
  },
};
```

#### 2. SEO评分标准
- **92-95分**：优秀（缺少1-2个可选Schema）
- **96-99分**：卓越（所有必需Schema完整）
- **100分**：完美（包含所有Schema + 用户评分 + 视频）

---

## 页面结构规范

### 1. 隐藏的H1标签（必需）

**为什么隐藏？**
- ✅ SEO需要H1，Google要求每个页面有唯一H1
- ✅ 用户不需要看到，避免信息冗余
- ✅ 保持界面简洁

**实现方式：**
```tsx
export default function CalculatorPage() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-gray-50 to-white">
      {/* 结构化数据 */}
      <script type="application/ld+json" {...} />
      
      {/* ✅ 隐藏的H1标签 - SEO可见，用户不可见 */}
      <h1 className="sr-only">
        Calculator Name - Complete SEO-Friendly Title with Keywords
      </h1>
      
      {/* 面包屑导航 */}
      <nav aria-label="Breadcrumb">...</nav>
      
      {/* 计算器组件 - 用户直接看到这个 */}
      <CalculatorComponent />
      
      {/* 教育内容 */}
      <section>...</section>
    </div>
  );
}
```

### 2. 面包屑导航（必需）

**三层级结构：**
```
Home / Category / Calculator Name
```

**完整实现：**
```tsx
<nav aria-label="Breadcrumb" className="bg-white border-b border-gray-200">
  <div className="container mx-auto px-4 py-3">
    <ol className="flex items-center space-x-2 text-sm text-gray-600" 
        itemScope itemType="https://schema.org/BreadcrumbList">
      {/* 第1层：Home */}
      <li itemProp="itemListElement" itemScope itemType="https://schema.org/ListItem">
        <a href="/" itemProp="item" className="hover:text-blue-600 transition-colors">
          <span itemProp="name">Home</span>
        </a>
        <meta itemProp="position" content="1" />
      </li>
      <li className="text-gray-400">/</li>
      
      {/* 第2层：Category */}
      <li itemProp="itemListElement" itemScope itemType="https://schema.org/ListItem">
        <a href="/financial" itemProp="item" className="hover:text-blue-600 transition-colors">
          <span itemProp="name">Financial</span>
        </a>
        <meta itemProp="position" content="2" />
      </li>
      <li className="text-gray-400">/</li>
      
      {/* 第3层：Current Calculator */}
      <li itemProp="itemListElement" itemScope itemType="https://schema.org/ListItem">
        <span itemProp="name" className="text-gray-900 font-semibold">
          Calculator Name
        </span>
        <meta itemProp="position" content="3" />
      </li>
    </ol>
  </div>
</nav>
```

**分类映射：**
| 分类名称 | URL路径 | 包含的计算器示例 |
|---------|---------|-----------------|
| **Financial** | `/financial` | Mortgage, Loan, Tax, Interest, Savings |
| **Health & Fitness** | `/health-fitness` | BMI, Calorie, TDEE, Body Fat |
| **Math & Numbers** | `/math-numbers` | Percentage, Average, Fraction |
| **Date & Time** | `/date-time` | Age, Date, Time |
| **Other** | `/other` | Tip, Unit Converter, Square Footage |

### 3. 内容结构层次

```tsx
return (
  <div>
    {/* 1. JSON-LD 结构化数据 */}
    <script type="application/ld+json" {...} />
    
    {/* 2. 隐藏的 H1 */}
    <h1 className="sr-only">...</h1>
    
    {/* 3. 面包屑导航 */}
    <nav>...</nav>
    
    {/* 4. 计算器组件 */}
    <CalculatorComponent />
    
    {/* 5. 教育内容（2000+字） */}
    <section className="max-w-7xl mx-auto px-4 py-12">
      <h2>Understanding [Topic]</h2>
      <p>详细的教育内容...</p>
      {/* ... 更多内容 ... */}
    </section>
    
    {/* 6. 相关计算器推荐 */}
    <section className="max-w-7xl mx-auto px-4 py-12 border-t">
      <h2>Related Calculators</h2>
      {/* ... 推荐链接 ... */}
    </section>
  </div>
);
```

---

## 结构化数据规范

### 5个核心Schema（必需）

#### 1. WebApplication Schema
```tsx
{
  '@type': 'WebApplication',
  '@id': getWebAppId('/calculator-path'),
  name: 'Calculator Name',
  url: getUrl('/calculator-path'),
  description: '详细描述计算器功能...',
  applicationCategory: 'FinanceApplication',
  operatingSystem: 'Any',
  featureList: [
    'Feature 1',
    'Feature 2',
    'Feature 3',
    // 5-8个核心功能
  ],
  offers: {
    '@type': 'Offer',
    price: '0',
    priceCurrency: 'USD',
  },
}
```

#### 2. BreadcrumbList Schema
```tsx
{
  '@type': 'BreadcrumbList',
  '@id': getBreadcrumbId('/calculator-path'),
  itemListElement: [
    {
      '@type': 'ListItem',
      position: 1,
      name: 'Home',
      item: getUrl('/'),
    },
    {
      '@type': 'ListItem',
      position: 2,
      name: 'Category',
      item: getUrl('/category'),
    },
    {
      '@type': 'ListItem',
      position: 3,
      name: 'Calculator Name',
      item: getUrl('/calculator-path'),
    },
  ],
}
```

#### 3. FAQPage Schema
```tsx
{
  '@type': 'FAQPage',
  '@id': getFaqId('/calculator-path'),
  mainEntity: [
    {
      '@type': 'Question',
      name: 'What is [Calculator] and how does it work?',
      acceptedAnswer: {
        '@type': 'Answer',
        text: '150-200字的简洁答案...'
      }
    },
    // 5-6个高质量FAQ
  ],
}
```

#### 4. HowTo Schema
```tsx
{
  '@type': 'HowTo',
  '@id': getHowToId('/calculator-path'),
  name: 'How to Use [Calculator]',
  description: '简要说明使用步骤',
  totalTime: 'PT5M',
  estimatedCost: {
    '@type': 'MonetaryAmount',
    currency: 'USD',
    value: '0',
  },
  tool: {
    '@type': 'HowToTool',
    name: 'Calculator Name',
  },
  step: [
    {
      '@type': 'HowToStep',
      position: 1,
      name: 'Enter Input Data',
      text: '详细步骤说明...',
      url: getStepUrl('/calculator-path', 1),
    },
    // 6-8个步骤
  ],
}
```

#### 5. Article Schema
```tsx
{
  '@type': 'Article',
  '@id': getArticleId('/calculator-path'),
  headline: 'Calculator Name - Complete Guide',
  description: '文章描述...',
  author: {
    '@type': 'Organization',
    name: 'AICalculator.pro',
    url: getUrl('/'),
  },
  publisher: {
    '@type': 'Organization',
    name: 'AICalculator.pro',
    logo: {
      '@type': 'ImageObject',
      url: getUrl('/logo.png'),
    },
  },
  datePublished: '2024-01-01',
  dateModified: '2024-11-16',
  image: getOgImage('calculator-name'),
  articleBody: '教育内容摘要...',
}
```

---

## FAQ撰写规范

### 核心原则：简洁 > 详细

**标准：**
- ✅ **数量**：5-6个（不超过8个）
- ✅ **每个长度**：150-200字
- ✅ **总字数**：800-1,000字
- ✅ **问题类型**：用户真实搜索的关键词
- ✅ **答案风格**：直接回答，不拖沓

### FAQ结构模板

```tsx
mainEntity: [
  // FAQ 1: 核心功能问题
  {
    '@type': 'Question',
    name: 'What is [Calculator Name] and how does it work?',
    acceptedAnswer: {
      '@type': 'Answer',
      text: '简洁解释计算器功能、工作原理、适用场景。包含关键公式或逻辑。约150-200字。'
    }
  },
  
  // FAQ 2: 使用方法问题
  {
    '@type': 'Question',
    name: 'How do I use the [Calculator Name]?',
    acceptedAnswer: {
      '@type': 'Answer',
      text: '简洁的使用步骤：1) 输入X 2) 选择Y 3) 点击计算 4) 查看结果。约150-200字。'
    }
  },
  
  // FAQ 3: 关键概念解释
  {
    '@type': 'Question',
    name: 'What is [Key Concept] and why is it important?',
    acceptedAnswer: {
      '@type': 'Answer',
      text: '解释关键概念、重要性、影响因素。包含简单示例。约150-200字。'
    }
  },
  
  // FAQ 4: 实际应用场景
  {
    '@type': 'Question',
    name: 'When should I use [Calculator Name]?',
    acceptedAnswer: {
      '@type': 'Answer',
      text: '适用场景、最佳实践、常见用例。约150-200字。'
    }
  },
  
  // FAQ 5: 结果解读问题
  {
    '@type': 'Question',
    name: 'How do I interpret the results?',
    acceptedAnswer: {
      '@type': 'Answer',
      text: '如何理解结果、什么是好/坏结果、下一步行动建议。约150-200字。'
    }
  },
  
  // FAQ 6: 权威来源或延伸（可选）
  {
    '@type': 'Question',
    name: 'Where can I find more information?',
    acceptedAnswer: {
      '@type': 'Answer',
      text: '提供权威网站链接（.gov, .edu）、相关工具推荐、延伸阅读。约150-200字。'
    }
  }
]
```

### ✅ 好的FAQ示例

```tsx
{
  '@type': 'Question',
  name: 'How is property tax calculated?',
  acceptedAnswer: {
    '@type': 'Answer',
    text: 'Property tax is calculated using the formula: Assessed Value × Tax Rate. The assessed value is typically a percentage of your home\'s market value (e.g., 80-100%), set by your local assessor. The tax rate is expressed as a percentage or per $1,000 of assessed value. For example, a $400,000 home with 100% assessment ratio and 1.2% tax rate would pay $4,800 annually ($400,000 × 1.2% = $4,800). Rates vary significantly by location, from 0.3% in Hawaii to 2.5% in New Jersey. Visit your county assessor\'s website for local rates.'
    // ✅ 约180字 - 简洁、有数据、有示例、有来源
  }
}
```

---

## 内链和外链规范

### 外链规范（3-5个）

**选择原则：**
1. ✅ **优先链接权威网站**：
   - 政府网站 (.gov) - IRS.gov, SSA.gov, Treasury.gov
   - 教育机构 (.edu) - 大学研究、学术论文
   - 权威金融机构 - Federal Reserve, World Bank
   - 行业协会 - AICPA, CFA Institute

2. ✅ **按主题选择**：
   - 税务类 → IRS.gov 相关税法页面
   - 社保类 → SSA.gov 官方资源
   - 房产类 → HUD.gov, Fannie Mae
   - 投资类 → SEC.gov, FINRA.org
   - 健康类 → CDC.gov, NIH.gov

3. ✅ **外链最佳实践**：
   - 每页3-5个高质量外链
   - 自然融入教育内容中
   - 使用描述性锚文本
   - 添加 `rel="noopener noreferrer"`
   - 在新标签打开 `target="_blank"`

**代码示例：**
```tsx
<p className="text-gray-700 mb-4">
  For official capital gains tax rates and regulations, visit the{' '}
  <a 
    href="https://www.irs.gov/taxtopics/tc409" 
    target="_blank" 
    rel="noopener noreferrer"
    className="text-blue-600 hover:text-blue-800 underline"
  >
    IRS Capital Gains Tax Guide
  </a>
  {' '}for the most up-to-date information.
</p>
```

### 内链规范（4-8个）

**选择原则：**
1. ✅ **链接到真正相关的计算器**
2. ✅ **按主题动态选择**
3. ✅ **流程相关性**

**示例场景：**

**Capital Gains Tax Calculator 应该链接到：**
- Investment Calculator（投资回报）
- ROI Calculator（投资收益率）
- Retirement Calculator（退休规划）
- Tax Calculator（综合税务）
- Inflation Calculator（通胀影响）

**Property Tax Calculator 应该链接到：**
- Mortgage Calculator（房贷计算）
- Home Affordability Calculator（购房能力）
- Rent Calculator（租vs买对比）
- Home Loan Calculator（房屋贷款）

**代码示例：**

**方式1：内容中自然提及**
```tsx
<p className="text-gray-700 mb-4">
  Before calculating capital gains tax, you may want to use our{' '}
  <a href="/roi-calculator" className="text-blue-600 hover:text-blue-800 underline">
    ROI Calculator
  </a>
  {' '}to determine your investment's total return.
</p>
```

**方式2：页面底部"相关计算器"章节**
```tsx
<section className="max-w-7xl mx-auto px-4 py-12 border-t border-gray-200">
  <h2 className="text-2xl font-bold text-gray-900 mb-6">Related Calculators</h2>
  <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
    <a 
      href="/investment-calculator" 
      className="block p-4 bg-white border border-gray-200 rounded-lg hover:shadow-md transition-shadow"
    >
      <div className="text-3xl mb-2">📊</div>
      <h3 className="font-semibold text-gray-900">Investment Calculator</h3>
      <p className="text-sm text-gray-600 mt-1">Calculate investment returns and growth</p>
    </a>
    
    {/* 更多相关计算器... */}
  </div>
</section>
```

---

## UI设计规范

### 配色方案：蓝灰色系

**核心原则：专业、统一、克制**

#### 主色调 - 蓝色
```tsx
const PRIMARY = {
  bg: 'bg-blue-600',              // 主要按钮背景
  bgHover: 'bg-blue-700',         // 主要按钮悬停
  text: 'text-blue-600',          // 链接、强调文字
  textHover: 'text-blue-700',     // 链接悬停
  border: 'border-blue-200',      // 边框
  light: 'bg-blue-50',            // 浅色背景
  gradient: 'from-blue-50 to-indigo-50', // 渐变背景
};
```

#### 中性色 - 灰色
```tsx
const NEUTRAL = {
  text: 'text-gray-900',          // 主要文字
  textSecondary: 'text-gray-600', // 次要文字
  textMuted: 'text-gray-500',     // 辅助文字
  border: 'border-gray-200',      // 边框
  bg: 'bg-white',                 // 卡片背景
  bgLight: 'bg-gray-50',          // 浅色背景
};
```

#### 状态色（谨慎使用）
```tsx
const STATUS = {
  success: 'bg-green-50 border-green-200 text-green-700',
  warning: 'bg-amber-50 border-amber-200 text-amber-700',
  error: 'bg-red-50 border-red-200 text-red-700',
  info: 'bg-blue-50 border-blue-200 text-blue-700',
};
```

### ✅ 标准UI模板

```tsx
<div className="max-w-7xl mx-auto px-4 py-6">
  <div className="grid grid-cols-1 xl:grid-cols-3 gap-6">
    {/* 输入区域 */}
    <div className="xl:col-span-1">
      <Card className="shadow-lg">
        {/* ✅ 统一的蓝色渐变背景 */}
        <CardHeader className="bg-gradient-to-r from-blue-50 to-indigo-50">
          <CardTitle className="text-xl text-gray-900">Calculator Inputs</CardTitle>
        </CardHeader>
        <CardContent className="p-6 space-y-4">
          {/* 输入字段 */}
          <div>
            <Label className="text-sm font-medium text-gray-700">
              Field Name <span className="text-red-500">*</span>
            </Label>
            <input
              className="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500"
              placeholder="Enter value"
            />
            <p className="text-xs text-gray-500 mt-1">Helper text</p>
          </div>
          
          {/* ✅ 统一的蓝色按钮 */}
          <Button className="w-full bg-blue-600 hover:bg-blue-700 text-white py-3">
            Calculate
          </Button>
        </CardContent>
      </Card>
    </div>
    
    {/* 结果区域 */}
    <div className="xl:col-span-2">
      <Card className="shadow-lg">
        <CardHeader className="bg-gradient-to-r from-blue-50 to-indigo-50">
          <CardTitle className="text-xl text-gray-900">Results</CardTitle>
        </CardHeader>
        <CardContent className="p-6 space-y-4">
          {/* ✅ 蓝色系结果卡片 */}
          <div className="bg-blue-50 border-2 border-blue-200 rounded-lg p-4">
            <p className="text-xs text-gray-600 mb-1">Result Label:</p>
            <p className="text-3xl font-bold text-blue-700">$1,234</p>
          </div>
        </CardContent>
      </Card>
    </div>
  </div>
</div>
```

### ❌ 避免使用
- ❌ 紫色（`purple`）- 除非品牌色
- ❌ 粉色（`pink`）- 不够专业
- ❌ 橙色（`orange`）- 仅用于警告
- ❌ 多种颜色混用 - 降低专业感

---

## 移动端响应式设计

### 响应式布局标准

#### 1. 容器布局
```tsx
<div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
  <div className="grid grid-cols-1 xl:grid-cols-3 gap-6">
    <div className="xl:col-span-1">  {/* 输入表单 */}
    <div className="xl:col-span-2">  {/* 结果区域 */}
  </div>
</div>
```

#### 2. 响应式字号
```tsx
<h2 className="text-xl sm:text-2xl md:text-3xl font-bold">
  Title Text
</h2>

<div className="text-xl sm:text-2xl md:text-3xl lg:text-4xl xl:text-5xl font-bold break-all">
  $1,000,000+
</div>
```

#### 3. 响应式表格
```tsx
<div className="overflow-x-auto overflow-y-hidden">
  <table className="w-full min-w-[350px] text-xs sm:text-sm">
    <th className="px-2 sm:px-3 md:px-4 py-2 sm:py-3">
    {/* 表格内容 */}
  </table>
</div>
```

#### 4. 响应式图表
```tsx
<ResponsiveContainer width="100%" height={200} minHeight={180}>
  <LineChart data={data}>
    {/* 图表配置 */}
  </LineChart>
</ResponsiveContainer>
```

#### 5. 响应式按钮
```tsx
<button className="px-4 py-3 text-sm sm:text-base min-h-[44px] font-medium">
  Action Button
</button>
```

### 移动端开发检查清单
- [ ] 所有文本在最小屏幕 (375px) 上可读
- [ ] 所有按钮 >= 44px 高度
- [ ] 表格可以水平滚动
- [ ] 大金额数字使用 `break-all` 防止溢出
- [ ] 没有水平页面滚动（除表格外）
- [ ] 在 iPhone SE (375x667) 上测试通过

---

## 输入字段设计规范

### 核心原则：透明度 > 简洁性

**设计原则：**
1. ✅ **透明度优先** - 显示所有参与计算的字段
2. ✅ **清晰标注** - 明确标记必填/可选
3. ✅ **合理默认值** - 可选字段都有智能默认值
4. ✅ **不隐藏信息** - 避免用户不知道背后用了什么参数

### 字段标注规范

**必填字段：**
```tsx
<Label htmlFor="homePrice" className="text-sm font-medium">
  Home Price <span className="text-red-500">*</span>
</Label>
<input
  id="homePrice"
  type="number"
  className="w-full px-4 py-3 border border-gray-300 rounded-lg"
  placeholder="400000"
  required
/>
```

**可选字段：**
```tsx
<Label htmlFor="assessmentRatio" className="text-sm font-medium">
  Assessment Ratio (%) <span className="text-gray-500 text-xs">- Optional</span>
</Label>
<input
  id="assessmentRatio"
  type="number"
  value="100"
  className="w-full px-4 py-3 border border-gray-300 rounded-lg"
  placeholder="100"
/>
<p className="text-xs text-gray-500">Default: 100% (full home value)</p>
```

### 输入框布局

**响应式标签布局：**
```tsx
<div className="flex flex-col sm:flex-row sm:items-center gap-2 sm:gap-3">
  <label className="text-sm font-medium text-gray-700 sm:w-32 flex items-center sm:flex-shrink-0">
    Field Name
  </label>
  <input className="w-full px-4 py-3 border border-gray-300 rounded-lg" />
</div>
```

### 检查清单
- [ ] 所有参与计算的字段都显示出来
- [ ] 必填字段用红色星号标注
- [ ] 可选字段用灰色"Optional"标注
- [ ] 每个可选字段都有合理的默认值
- [ ] 每个字段下方有说明文字
- [ ] **计算按钮在所有输入字段的最底部**

---

## 分享和导出功能

### 3个必需功能

1. ✅ **分享功能** - 使用 `useShare` Hook + `ShareModal` 组件
2. ✅ **保存为图片** - 使用 `html2canvas` 库
3. ✅ **打印功能** - 将结果打印为 PDF

### 完整实现代码

```tsx
'use client';

import { useState, useRef } from 'react';
import { Button } from '@/components/ui/button';
import { Download, Share2, Printer } from 'lucide-react';
import html2canvas from 'html2canvas';
import { ShareModal } from '@/components/ShareModal';
import { useShare } from '@/hooks/useShare';

export default function YourCalculator() {
  const [result, setResult] = useState(null);
  
  // ✅ 结果区域的引用
  const resultRef = useRef<HTMLDivElement>(null);
  
  // ✅ 分享功能 Hook
  const { showShareModal, shareUrl, shareText, handleShare, closeShareModal } = useShare({
    calculatorPath: '/your-calculator',
    getShareParams: () => ({
      n: inputs.number?.toString() || '',
      r: inputs.rate?.toString() || '',
    }),
    getShareText: () => {
      return result 
        ? `My calculation result: ${result.value}`
        : 'Check out this calculator!';
    },
  });

  // ✅ 保存为图片
  const handleSaveAsImage = async () => {
    if (!resultRef.current) return;
    
    try {
      await new Promise(resolve => setTimeout(resolve, 500));
      
      const canvas = await html2canvas(resultRef.current, {
        scale: 2,
        backgroundColor: '#ffffff',
        logging: false,
        useCORS: true,
        allowTaint: true,
      });
      
      const link = document.createElement('a');
      link.download = `calculator-result-${new Date().toISOString().split('T')[0]}.png`;
      link.href = canvas.toDataURL('image/png', 1.0);
      link.click();
    } catch (error) {
      console.error('Error saving image:', error);
      alert('Failed to save image. Please try again.');
    }
  };

  // ✅ 打印功能
  const handlePrint = async () => {
    if (!resultRef.current) return;
    
    try {
      await new Promise(resolve => setTimeout(resolve, 500));
      
      const canvas = await html2canvas(resultRef.current, {
        scale: 2,
        backgroundColor: '#ffffff',
        logging: false,
      });
      
      const imgData = canvas.toDataURL('image/png');
      const printWindow = window.open('', '_blank');
      if (printWindow) {
        printWindow.document.write(`
          <html>
            <head>
              <title>Calculator Results</title>
              <style>
                body { margin: 0; padding: 20px; display: flex; justify-content: center; }
                img { max-width: 100%; height: auto; }
                @media print {
                  body { padding: 0; }
                  img { max-width: 100%; page-break-inside: avoid; }
                }
              </style>
            </head>
            <body>
              <img src="${imgData}" onload="window.print();"/>
            </body>
          </html>
        `);
        printWindow.document.close();
      }
    } catch (error) {
      console.error('Failed to print:', error);
      alert('Failed to print. Please try again.');
    }
  };

  return (
    <div className="max-w-7xl mx-auto px-4 py-6">
      {/* 输入区域 */}
      
      {/* ✅ 结果区域 - 添加 ref */}
      <div ref={resultRef}>
        <Card className="shadow-lg">
          <CardContent className="p-6">
            {/* 结果内容 */}
          </CardContent>
        </Card>
      </div>

      {/* ✅ 操作按钮区域 */}
      <div className="flex flex-wrap gap-3 justify-center mt-6 print:hidden">
        <Button 
          onClick={handleSaveAsImage} 
          variant="outline" 
          className="gap-2"
          disabled={!result}
        >
          <Download className="h-4 w-4" />
          Save as Image
        </Button>
        
        <Button 
          onClick={handlePrint} 
          variant="outline" 
          className="gap-2"
          disabled={!result}
        >
          <Printer className="h-4 w-4" />
          Print Results
        </Button>
        
        <Button 
          onClick={handleShare} 
          variant="outline" 
          className="gap-2"
        >
          <Share2 className="h-4 w-4" />
          Share Calculator
        </Button>
      </div>

      {/* ✅ 分享模态框 */}
      <ShareModal 
        isOpen={showShareModal}
        onClose={closeShareModal}
        shareUrl={shareUrl}
        shareText={shareText}
        calculatorName="Your Calculator Name"
      />
    </div>
  );
}
```

### 检查清单
- [ ] 导入 `useShare` Hook 和 `ShareModal` 组件
- [ ] 导入 `html2canvas` 库
- [ ] 创建 `resultRef` 引用结果区域
- [ ] 实现 `handleSaveAsImage` 函数
- [ ] 实现 `handlePrint` 函数
- [ ] 添加3个操作按钮
- [ ] 按钮有正确的 `disabled` 状态
- [ ] 结果区域使用 `ref={resultRef}`

---

## 域名配置规范

### 核心原则：禁止硬编码URL！

**生产环境域名：** `https://aicalculator.pro`

### 必须使用配置文件

**步骤1：导入辅助函数**
```tsx
import { 
  getUrl, 
  getOgImage, 
  getBreadcrumbId, 
  getWebAppId, 
  getFaqId, 
  getHowToId, 
  getArticleId, 
  getStepUrl 
} from '@/config/site';
```

**步骤2：替换 metadata 中的URL**
```tsx
export const metadata: Metadata = {
  openGraph: {
    url: getUrl('/calculator-path'),        // ✅ 使用配置
    images: [{
      url: getOgImage('calculator-name'),   // ✅ 使用配置
    }],
  },
  alternates: {
    canonical: getUrl('/calculator-path'),  // ✅ 使用配置
  },
};
```

**步骤3：替换 JSON-LD 中的URL**
```tsx
const jsonLd = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'WebApplication',
      '@id': getWebAppId('/calculator-path'),  // ✅ 使用配置
      url: getUrl('/calculator-path'),         // ✅ 使用配置
    },
    {
      '@type': 'BreadcrumbList',
      '@id': getBreadcrumbId('/calculator-path'), // ✅ 使用配置
      itemListElement: [
        {
          item: getUrl('/'),                  // ✅ 使用配置
        },
      ],
    },
  ],
};
```

### ❌ 禁止的做法
```tsx
// ❌ 禁止！硬编码 URL
"url": "https://aicalculator.pro/calculator-path"
"canonical": "https://aicalculator.pro/calculator-path"
```

### 检查清单
- [ ] 导入了 `config/site.ts` 的辅助函数
- [ ] 替换了所有硬编码URL（20-30处）
- [ ] 保留了所有20+个关键词
- [ ] 保留了所有SEO内容
- [ ] 代码中不包含 `https://aicalculator.pro` 字符串

---

## 计算器注册（⚠️ 必须！）

### 为什么必须注册到数据库？

每次创建新计算器后，**必须**将其添加到 `lib/calculatorData.ts` 中，否则：
- ❌ 首页搜索框**完全搜索不到**新计算器
- ❌ All Calculator Tools 页面无法找到
- ❌ 相关推荐功能无法生效

### 注册步骤

**修改文件：** `lib/calculatorData.ts`

**添加位置：** 在对应分类的计算器列表中（Financial, Health, Math 等）

**代码示例：**
```typescript
// lib/calculatorData.ts

export const allCalculators: Calculator[] = [
  // Financial Calculators
  { name: 'Mortgage Calculator', url: '/mortgage-calculator', keywords: ['home loan', 'house payment'], category: 'Financial', icon: '🏠' },
  
  // ✅ 新计算器添加在这里
  { 
    name: 'Rent vs Buy Calculator',  // 计算器名称
    url: '/rent-vs-buy-calculator',   // URL路径
    keywords: [                        // 搜索关键词（15-20个）
      'rent or buy',
      'renting vs buying',
      'home affordability',
      'rent vs mortgage',
      'should i rent or buy',
      'homeownership cost',
      'break-even calculator',
      'home equity',
      'housing decision',
      // ... 更多关键词
    ],
    category: 'Financial',             // 分类
    icon: '🏘️'                        // 图标
  },
  
  { name: 'Loan Calculator', url: '/loan-calculator', keywords: ['borrow', 'lending'], category: 'Financial', icon: '💰' },
  // ... 其他计算器
];
```

### 字段说明

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| **name** | string | 计算器显示名称 | `'Rent vs Buy Calculator'` |
| **url** | string | URL路径（必须以 `/` 开头） | `'/rent-vs-buy-calculator'` |
| **keywords** | string[] | 搜索关键词数组（15-20个） | `['rent or buy', 'housing decision']` |
| **category** | string | 分类（Financial/Health/Math/Other） | `'Financial'` |
| **icon** | string | 显示图标（emoji） | `'🏘️'` |

### 关键词选择技巧

**原则：**
1. ✅ 包含用户真实搜索的词汇
2. ✅ 覆盖不同表达方式（rent vs buy / renting vs buying）
3. ✅ 包含长尾关键词（should i rent or buy a house）
4. ✅ 包含相关概念（home equity, break-even）

**示例：Rent vs Buy Calculator**
- 核心词：`rent vs buy`, `renting vs buying`
- 疑问词：`should i rent or buy`, `rent or buy calculator`
- 长尾词：`cost of renting vs buying`, `home affordability`
- 相关词：`break-even calculator`, `home equity`, `homeownership cost`

### 测试验证

完成注册后，必须测试以下功能：

**1. 首页搜索框测试**
- [ ] 在首页搜索框输入计算器名称，能找到
- [ ] 输入关键词（如 "rent or buy"），能找到
- [ ] 点击搜索结果，能正确跳转

**2. All Calculator Tools 页面测试**
- [ ] 访问 `/calculators` 页面
- [ ] 在搜索框输入计算器名称，能找到
- [ ] 在对应分类中能看到新计算器

**3. 分类页面测试**
- [ ] 访问分类页面（如 `/financial`）
- [ ] 能在列表中看到新计算器
- [ ] 点击能正确跳转

---

## 开发检查清单

### 计算器注册检查（⚠️ 第一步！）
- [ ] 已添加到 `lib/calculatorData.ts`
- [ ] name 字段正确
- [ ] url 字段正确（以 `/` 开头）
- [ ] keywords 包含15-20个相关关键词
- [ ] category 分类正确
- [ ] icon 图标已设置
- [ ] 首页搜索框能搜索到
- [ ] All Calculator Tools 页面能找到

### SEO检查（必需100分）
- [ ] 完整的 metadata 配置（title, description, keywords）
- [ ] 20+个关键词
- [ ] 5个核心 Schema（WebApp, Breadcrumb, FAQ, HowTo, Article）
- [ ] 5-6个高质量 FAQ（总字数800-1,000字）
- [ ] 隐藏的 H1 标签使用 `sr-only`
- [ ] 面包屑导航（三层级结构）
- [ ] 3-5个权威外链（.gov, .edu）
- [ ] 4-8个相关内链
- [ ] Open Graph 和 Twitter Card 完整
- [ ] Canonical URL 配置

### UI检查（蓝灰配色）
- [ ] 主色调统一使用蓝色（`blue-*`）
- [ ] 卡片头部使用 `from-blue-50 to-indigo-50`
- [ ] 按钮使用 `bg-blue-600 hover:bg-blue-700`
- [ ] 文字颜色使用灰色系
- [ ] 没有使用紫色、粉色、橙色（除必要状态色）

### 功能检查（3个导出功能）
- [ ] 实现分享功能（`useShare` Hook + `ShareModal`）
- [ ] 实现保存图片功能（`html2canvas`）
- [ ] 实现打印功能
- [ ] 结果区域使用 `ref={resultRef}`
- [ ] 按钮有正确的 `disabled` 状态

### 移动端检查
- [ ] 所有文本在 375px 宽度下可读
- [ ] 所有按钮高度 >= 44px
- [ ] 表格可以水平滚动
- [ ] 大数字使用 `break-all`
- [ ] 响应式字号、间距、布局
- [ ] 在 iPhone SE 上测试通过

### 输入字段检查
- [ ] 必填字段用红色星号标注
- [ ] 可选字段用灰色"Optional"标注
- [ ] 所有可选字段有默认值
- [ ] 字段下方有说明文字
- [ ] 计算按钮在最底部

### 配置检查
- [ ] 使用 `config/site.ts` 的辅助函数
- [ ] 没有硬编码 URL
- [ ] 所有图片使用 `getOgImage()`
- [ ] 所有链接使用 `getUrl()`

### 内容检查
- [ ] 2000+字教育内容
- [ ] 相关计算器推荐章节
- [ ] 内容分段清晰（H2, H3, H4层级）
- [ ] 包含实际案例和示例

---

## 总结

### 核心要求
1. ✅ **SEO满分** - 5个Schema + 5-6个FAQ + 完整metadata
2. ✅ **蓝灰配色** - 专业统一的UI设计
3. ✅ **3个导出功能** - 分享、保存图片、打印
4. ✅ **移动端优先** - 响应式设计，375px起
5. ✅ **透明设计** - 所有输入字段清晰展示
6. ✅ **配置化URL** - 禁止硬编码，使用配置文件

### 质量标准
- **SEO评分**：96-100分（卓越-完美）
- **移动端适配**：iPhone SE (375px) 测试通过
- **代码规范**：使用统一组件和Hook
- **用户体验**：专业、可信、易用

---

**文档版本：** v1.0  
**最后更新：** 2024-11-17  
**维护团队：** AICalculator.pro Development Team

